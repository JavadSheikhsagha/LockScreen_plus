<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ConstraintSet android:id="@+id/start_notif_rv">
        <Constraint android:id="@+id/container_notif_rv_ll"
            android:layout_marginEnd="12dp" />
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end_notif_rv">
        <Constraint android:id="@id/container_notif_rv_ll"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_width="match_parent"
            app:layout_constraintTop_toTopOf="parent"
            android:alpha="1" />
    </ConstraintSet>

    <Transition
        app:constraintSetEnd="@id/end_notif_rv"
        app:constraintSetStart="@+id/start_notif_rv"
        app:duration="800"
        />
</MotionScene>
